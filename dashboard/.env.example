# Supabase project (auth happens in the browser + SSR session cookies)
PUBLIC_SUPABASE_URL=http://45.129.182.185:8100
PUBLIC_SUPABASE_ANON_KEY=

# FastAPI backend — server-side only, never exposed to the browser
PRIVATE_API_URL=http://localhost:8000
